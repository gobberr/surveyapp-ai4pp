#!/usr/bin/expect -f

set timeout -1;

spawn ssh -o StrictHostKeyChecking=no -i /tmp/key $env(DEV_USER)@$env(DEV_IP) "bash hera/deploy.sh $env(APP_NAME) $env(CI_COMMIT_SHORT_SHA)"

expect "Enter passphrase for key '/tmp/key':"

send "$env(DEV_PASS)\n"

expect "complete"
